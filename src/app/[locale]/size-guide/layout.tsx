import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Size Guide",
  description: "Find your perfect fit with the NOCTURNE size guide. How to measure bust, waist, and hips. Size chart XS–4XL. Bra sizing guide.",
};

export default function SizeGuideLayout({ children }: { children: React.ReactNode }) {
  return children;
}
